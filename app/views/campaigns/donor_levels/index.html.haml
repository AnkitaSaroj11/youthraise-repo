= content_for :sidebar do
  = render '/campaigns/sidebar', campaign: @campaign
= render '/campaigns/alerts', campaign: @campaign
= render '/campaigns/header', campaign: @campaign
.px-4.sm:px-6.lg:px-8
  .sm:flex.sm:items-center
    .sm:flex-auto
      %h1.text-base.font-semibold.leading-6.text-gray-900 Donor Levels & Perks
      %p.mt-2.text-sm.text-gray-700 Donor levels encourage members of your community to contribute to your fundraiser. Be creative - examples of great perks include preferred seating at an event, like a Christmas concert, or free tickets to a pancake breakfast or spaghetti dinner.
    -# .mt-4.sm:mt-0.sm:ml-16.sm:flex-none
    -#   = link_to "Add Donor Level", new_campaign_donor_level_path(@campaign), data: {'turbo-frame': "donor_levels"}, class: "block rounded-md bg-indigo-600 py-2 px-3 text-center text-sm font-semibold text-white shadow-sm hover:bg-indigo-500 focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-indigo-600", type: "button"
  = turbo_frame_tag "donor_levels" do
    %ul.mt-4.grid.grid-cols-1.gap-6.sm:grid-cols-2.md:grid-cols-3.lg:grid-cols-3{:role => "list"}
      = render @donor_levels, admin: true
